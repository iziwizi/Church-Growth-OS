{
  "name": "@church-growth-os/functions",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@church-growth-os/ai": "workspace:*",
    "@church-growth-os/automation": "workspace:*",
    "@church-growth-os/communication": "workspace:*",
    "@church-growth-os/shared": "workspace:*",
    "firebase-admin": "^12.4.0",
    "firebase-functions": "^5.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
